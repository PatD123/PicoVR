/**
 * @brief
 * The following rasterization methods rasterize triangles
 * to a framebuffer.
 */

#include "engine/rasterize.h"

void rasterize(const triangle32_t *t0)
{
    printf("hi %i", FRAMEBUFFER[0][0]);
}